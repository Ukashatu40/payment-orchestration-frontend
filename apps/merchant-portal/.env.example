# The real NestJS backend's origin (no trailing slash, no /api/v1 suffix).
# Used server-side only (Server Components, Route Handlers) — never exposed
# to the browser, which must always talk to this app's own origin instead.
BACKEND_API_URL=http://localhost:3000
